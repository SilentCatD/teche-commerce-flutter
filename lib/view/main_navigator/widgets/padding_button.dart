import 'package:flutter/material.dart';

class PaddingButton extends StatelessWidget {
  const PaddingButton({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      width: 50,
      height: 50
      );
  }
}
